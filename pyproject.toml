[project]
name = "ag-vis"
version = "0.1.0"
description = "Animated visualization for AI agent activity"
requires-python = ">=3.12,<3.13"
dependencies = [
    "laya==0.3.4",
]

[tool.uv]
package = false
